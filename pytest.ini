[pytest]
minversion = 7.0
addopts = -ra -q --tb=short --browser chromium
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Playwright options
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
